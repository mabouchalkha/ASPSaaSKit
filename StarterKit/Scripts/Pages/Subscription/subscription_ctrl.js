angular.module('starterKit').controller('subscriptionController', ['$state', 'notif', function ($state, notif) {
    var vm = this;

    var _init = function () {
     
    };

    
    _init();
}]);
